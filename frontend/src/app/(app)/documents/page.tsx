import DocumentsManager from '@/components/boutique/documents/DocumentsManager';

export const metadata = { title: 'Documents — Sahilley' };

export default function DocumentsPage() {
  return <DocumentsManager />;
}
