import ParametresManager from '@/components/boutique/parametres/ParametresManager';

export const metadata = { title: 'Paramètres — Sahilley' };

export default function ParametresPage() {
  return <ParametresManager />;
}
