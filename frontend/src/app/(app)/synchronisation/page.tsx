import ConflictsManager from '@/components/boutique/sync/ConflictsManager';

export const metadata = { title: 'Synchronisation — Sahilley' };

export default function SynchronisationPage() {
  return <ConflictsManager />;
}
